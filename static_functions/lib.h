// report_value() is an "interface" for this library that we WANT to expose to 
// the rest of the program to use... ideally an interface like this is more 
// stable in that it is less likely to change.

void report_value(double value);
// extern declares the global variable number (but does not define it, i.e. no 
// new space is allocated in memory, instead it uses the exsting global number 
// variable)
extern int number;

void add_one();
void print();
